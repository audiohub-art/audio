version: '3'

services:
  database:
    image: postgres:18
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: username
      POSTGRES_PASSWORD: password
      POSTGRES_DB: audio
    volumes:
      - data:/var/lib/postgresql

volumes:
  data:
